#include <iostream>
#include <math.h>
int main ()
{
double x,y,z,a,b,c,h;
std::cout << "Vvedite x: ";
std::cin >> x;
std::cout << "Vvedite y: ";
std::cin >> y;
std::cout << "Vvedite z: ";
std::cin >> z;
a = 1+pow(sin(x+y),2);
b = fabs( x - ((2*y)/(1+pow(x,2)*pow(y,2))) );
c = pow(x,fabs(y))+pow(cos(atan(1/z)),2);
h = a/b*c;
std::cout << "Rфesult result h= " << h << std::endl;
return 0;
}
